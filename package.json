{
  "name": "triton",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://myname.github.io/triton",
  "dependencies": {
    "firebase": "^5.5.6",
    "gh-pages": "^2.0.1",
    "react": "^16.6.0",
    "react-dom": "^16.6.0",
    "react-router-dom": "^4.3.1",
    "react-scripts": "2.0.5",
    "react-transition-group": "^2.5.0",
    "react-virtualized-auto-sizer": "^1.0.2",
    "react-window": "^1.3.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "npm run-script build && gh-pages -d build",
    "update": "git push && npm run-script build && gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
